{
  "name": "umami-protocol-metrics",
  "license": "ISC",
  "author": "Clonescody",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:remote": "graph codegen && graph build && graph deploy --product hosted-service umamidao/protocol-metrics",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.1",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
